#include "main.h"

void reverse_array(int *a, int n)
{
  int length = strlen(str);
  int i; int j;
  for(i=0; j=length -i; i < j; i++; j--)
    {
    char temp = str[i];
    str[i] = str[j];
    str[j] = temp;
    }
}
