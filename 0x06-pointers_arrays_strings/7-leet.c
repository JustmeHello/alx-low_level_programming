#include "main.h"

char *leet(char *)
{
for(int i = 0; str[i]; i++)
{
char currentChar = tolower(str[i]);
  if (currentChar == 'a')
    str[i] == '4';
  if (currentChar == 'e')
    str[i] == '3';
  if (currentChar == 'o')
    str[i] == '0';
  if (currentChar == 't')
    str[i] == '7';
  if (currentChar == 'l')
    str[i] == '7';
}
}
