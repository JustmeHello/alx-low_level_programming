#include <stdio.h>
/**
*prints alphabet letters 
* 
*desc: The function uses loop to operate
*Return: no return
*/
int main (void)
{
int letter;

for (letter = 'a'; letter <= 'z'; letter++)
putchar(letter);

return(0);
}
}
