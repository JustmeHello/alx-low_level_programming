#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: Print alphabets backwards
 *
 * Return: 0 (Success)
 */

int main(void)
{
  char x,z;
  for(x=0,x<=10,x++)
    {
     putchar('0'+x)
    }
  for(z='a', z<=5, z++)
    {
    putchar(z);
    }
  putchar(\n);
  return (0);
}
